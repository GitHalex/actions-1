name: My first Workflow

on: push

jobs:
  first_job: 
    runs-on: ubuntu-latest
    steps:
    - name: Welcome message
      run: echo "My first Github Action Job"
    
    - name: list files
      run: ls -la

    - name: Read file
      run: cat README.md